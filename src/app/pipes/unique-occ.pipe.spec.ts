import { UniqueOccPipe } from './unique-occ.pipe';

describe('UniqueOccPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueOccPipe();
    expect(pipe).toBeTruthy();
  });
});
